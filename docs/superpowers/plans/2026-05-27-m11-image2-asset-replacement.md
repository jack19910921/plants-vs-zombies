# M11 Image2 Asset Replacement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the current plant and board placeholder visuals with the user-provided ChatGPT image model assets.

**Architecture:** Keep the current Phaser and DOM overlay rendering paths. Add assets to the existing texture registry, extend asset presentation profiles with bottom-center anchors and card sizing, and draw the new board image behind the logical grid.

**Tech Stack:** TypeScript, Phaser 3, Vite, Vitest, CSS, PNG assets.

---

### Task 1: Asset Registry And Presentation Profiles

**Files:**
- Modify: `src/game/assets.ts`
- Modify: `src/game/assetPresentation.ts`
- Test: `src/game/assets.test.ts`
- Test: `src/game/assetPresentation.test.ts`

- [ ] **Step 1: Write failing tests**

Add `src/game/assets.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { BOARD_TEXTURE, PLANT_TEXTURES } from "./assets";
import type { PlantId } from "./types";

const expectedPlantAssetNames: Record<PlantId, string> = {
  sunflower: "image2-sunflower.png",
  peashooter: "image2-peashooter.png",
  wallnut: "image2-wallnut.png",
  snowpea: "image2-snowpea.png",
  potatomine: "image2-potatomine.png"
};

describe("asset texture registry", () => {
  it("points every plant to the M11 image2 generated asset", () => {
    Object.entries(expectedPlantAssetNames).forEach(([plantId, fileName]) => {
      expect(PLANT_TEXTURES[plantId as PlantId]).toContain(fileName);
    });
  });

  it("exposes the M11 image2 garden board texture", () => {
    expect(BOARD_TEXTURE).toContain("image2-garden-board.png");
  });
});
```

Extend `src/game/assetPresentation.test.ts` with tests for `fieldAnchorX`, `fieldAnchorY`, `cssBackgroundSize`, and `getBoardAssetPresentation()`.

- [ ] **Step 2: Verify tests fail**

Run: `npm test -- src/game/assets.test.ts src/game/assetPresentation.test.ts`

Expected: fail because `BOARD_TEXTURE`, anchor fields, card background sizing, and board presentation do not exist yet.

- [ ] **Step 3: Implement minimal code**

Update the texture registry to point at `src/assets/generated/m11/*.png`, add board texture export, and add presentation fields:

```ts
fieldAnchorX: 0.5,
fieldAnchorY: 1,
cssBackgroundSize: "contain"
```

Add a board presentation crop object for `GameScene`.

- [ ] **Step 4: Verify tests pass**

Run: `npm test -- src/game/assets.test.ts src/game/assetPresentation.test.ts`

Expected: pass.

### Task 2: Process And Register Assets

**Files:**
- Create: `src/assets/generated/m11/image2-sunflower.png`
- Create: `src/assets/generated/m11/image2-peashooter.png`
- Create: `src/assets/generated/m11/image2-wallnut.png`
- Create: `src/assets/generated/m11/image2-snowpea.png`
- Create: `src/assets/generated/m11/image2-potatomine.png`
- Create: `src/assets/generated/m11/image2-garden-board.png`
- Modify: `docs/asset-sources.md`

- [ ] **Step 1: Generate transparent plant assets**

Use a local one-off PNG processing script to flood-fill border-connected near-white pixels to alpha and crop the transparent result with a small margin.

- [ ] **Step 2: Copy the garden board**

Copy `/Users/jack/Desktop/006.png` to `src/assets/generated/m11/image2-garden-board.png`.

- [ ] **Step 3: Record sources**

Append source rows to `docs/asset-sources.md` with:

- Source: AI-generated by OpenAI ChatGPT image model
- Generation date: 2026-05-27
- Local file path
- Intended use
- Prompt summary

### Task 3: Render New Assets In Game And Cards

**Files:**
- Modify: `src/game/GameScene.ts`
- Modify: `src/game/worldPresentation.ts`
- Modify: `src/ui/domOverlay.ts`
- Modify: `src/styles.css`
- Test: `src/ui/domOverlay.test.ts`
- Test: `src/game/worldPresentation.test.ts`

- [ ] **Step 1: Write failing tests**

Add DOM overlay assertions that card styles include `--plant-size: contain`.

Add world presentation assertions that the M11 profiles keep sunflower tallest, wallnut sturdy, and potatomine low.

- [ ] **Step 2: Verify tests fail**

Run: `npm test -- src/ui/domOverlay.test.ts src/game/worldPresentation.test.ts`

Expected: fail before DOM style variable and profile tuning exist.

- [ ] **Step 3: Implement rendering changes**

Load `BOARD_TEXTURE`, draw it cropped inside the existing logical `BOARD`, set plant image origins from asset presentation, and use bottom-center field placement.

Update CSS to use `background-size: var(--plant-size, cover)`.

- [ ] **Step 4: Verify tests pass**

Run: `npm test -- src/ui/domOverlay.test.ts src/game/worldPresentation.test.ts`

Expected: pass.

### Task 4: Full Verification And Commit

**Files:**
- All changed files from Tasks 1-3

- [ ] **Step 1: Run unit tests**

Run: `npm test`

Expected: all tests pass.

- [ ] **Step 2: Run production build**

Run: `npm run build`

Expected: TypeScript and Vite build pass.

- [ ] **Step 3: Run whitespace check**

Run: `git diff --check`

Expected: no output.

- [ ] **Step 4: Browser smoke**

Start the dev server and verify the game opens with M11 plant card art, field plant art, and board art visible.

- [ ] **Step 5: Commit**

Run:

```bash
git add docs src
git commit -m "Add M11 image2 asset replacement"
```
